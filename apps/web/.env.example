# Copy to .env.local for local dev. Ports match infra/docker/docker-compose.yml,
# which publishes Postgres on 55432 and Redis on 6380 (loopback) to avoid clashes.
DATABASE_URL=postgresql://atlas:atlas@127.0.0.1:55432/atlas
REDIS_URL=redis://127.0.0.1:6380
